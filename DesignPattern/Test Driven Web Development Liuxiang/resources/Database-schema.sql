CREATE TABLE slot_detail_meter (

  slot_detail_meter_id int NOT NULL,
  hard_count double NOT NULL,
  soft_count double NOT NULL,
  drop_date DATE NOT NULL,
  PRIMARY KEY(slot_detail_meter_id)
);

